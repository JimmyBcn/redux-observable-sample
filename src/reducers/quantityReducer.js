const initialState = {
    quantity: 0
};

function quantityReducer (state = initialState, action) {
    switch(action.type)    {
        case
    }
    return state;
}

export default quantityReducer;